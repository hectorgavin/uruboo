<div class="task">
    <div class="title">${task.title}</div>
    <div class="description">${task.description}</div>
    <div class="customer">${task.customer.firstName}</div>
    <div class="comments">Comentarios (${task.comments.size()})</div>
</div>